version: 0.2

phases:
  build:
    commands:
      - echo "No build needed – uploading to S3"

artifacts:
  files:
    - "**/*"
  exclude-files:
    - "**/buildspec.yml"
    - "**/README.md"
    - "**/.git/**/*"
  base-directory: .
  discard-paths: no
  name: /
  destination: hygo.pdftohtml
